# Task Management API built with Express

A simple RESTful API for managing tasks.

## Endpoints

- `GET /api/tasks`: Get all tasks.
- `POST /api/tasks`: Add a new task. (Body: `{ "title": "Task title" }`)
